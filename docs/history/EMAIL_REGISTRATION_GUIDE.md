# ✉️ Регистрация и покупка по email - Инструкция

## 📋 Как это будет работать

### 1️⃣ Регистрация пользователя

```
Пользователь → Форма регистрации → Email подтверждение → Аккаунт активен
```

**Нужно:**
- Форма регистрации (email, пароль, имя)
- Подтверждение email (ссылка)
- Хранение пользователей в БД

### 2️⃣ Покупка пакета

```
Пользователь → Выбирает пакет → Платежная форма → Оплата → Email с подтверждением
```

**Процесс:**
1. Пользователь выбирает пакет (Базовый или Профессиональный)
2. Переход на страницу оплаты
3. Оплата через ЮKassa/Stripe
4. После успешной оплаты:
   - Email с чеком
   - Баланс пополняется автоматически
   - Email с инструкцией

---

## 🔧 Что нужно реализовать

### 1. Система аутентификации (Next-Auth / Supabase)

**Next-Auth** (рекомендуется):
```typescript
// Установка
npm install next-auth @auth/prisma-adapter

// app/api/auth/[...nextauth]/route.ts
import NextAuth from "next-auth"
import EmailProvider from "next-auth/providers/email"

export const authOptions = {
  providers: [
    EmailProvider({
      server: process.env.EMAIL_SERVER,
      from: process.env.EMAIL_FROM
    }),
  ],
  database: process.env.DATABASE_URL,
}

export const handler = NextAuth(authOptions)
```

**Или Supabase** (проще):
```bash
npm install @supabase/supabase-js
```

### 2. База данных PostgreSQL

**Таблицы:**

```sql
-- Пользователи
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email VARCHAR(255) UNIQUE NOT NULL,
  name VARCHAR(255),
  password_hash TEXT,
  email_verified BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Балансы
CREATE TABLE balances (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  credits INTEGER DEFAULT 0,
  package_name VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Транзакции
CREATE TABLE transactions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  section VARCHAR(50),
  model VARCHAR(100),
  credits_spent INTEGER,
  input_tokens INTEGER,
  output_tokens INTEGER,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Покупки
CREATE TABLE purchases (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  package_name VARCHAR(100),
  credits INTEGER,
  price_rub INTEGER,
  payment_id VARCHAR(255),
  status VARCHAR(50), -- pending, completed, failed
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 3. Платежная система (ЮKassa)

**API ЮKassa:**
```typescript
// app/api/payment/create/route.ts
import { YooCheckout } from '@a2seven/yoo-checkout';

const checkout = new YooCheckout({
  shopId: process.env.YOOKASSA_SHOP_ID!,
  secretKey: process.env.YOOKASSA_SECRET_KEY!
});

export async function POST(request: Request) {
  const { packageKey, userId } = await request.json();
  
  const pkg = SUBSCRIPTION_PACKAGES[packageKey];
  
  // Создаем платеж
  const payment = await checkout.createPayment({
    amount: {
      value: pkg.priceRub.toString(),
      currency: 'RUB'
    },
    confirmation: {
      type: 'redirect',
      return_url: `${process.env.NEXT_PUBLIC_URL}/payment/success`
    },
    description: `Пакет ${pkg.name}`,
    metadata: {
      userId,
      packageKey,
      credits: pkg.credits
    }
  });
  
  return Response.json({ paymentUrl: payment.confirmation.confirmation_url });
}
```

**Webhook для обработки платежей:**
```typescript
// app/api/payment/webhook/route.ts
export async function POST(request: Request) {
  const body = await request.json();
  
  if (body.event === 'payment.succeeded') {
    const { userId, packageKey, credits } = body.object.metadata;
    
    // Пополняем баланс в БД
    await addCreditsToUser(userId, credits);
    
    // Отправляем email
    await sendPurchaseEmail(userId, packageKey, credits);
  }
  
  return Response.json({ success: true });
}
```

### 4. Email уведомления (Resend / SendGrid)

**Resend** (рекомендуется):
```bash
npm install resend
```

```typescript
// lib/email.ts
import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

export async function sendPurchaseEmail(
  email: string,
  packageName: string,
  credits: number
) {
  await resend.emails.send({
    from: 'noreply@yourdomain.com',
    to: email,
    subject: `Пакет ${packageName} успешно активирован!`,
    html: `
      <h1>Спасибо за покупку!</h1>
      <p>Ваш пакет <strong>${packageName}</strong> активирован.</p>
      <p>Баланс: <strong>${credits} единиц</strong></p>
      <a href="${process.env.NEXT_PUBLIC_URL}/balance">Перейти в личный кабинет</a>
    `
  });
}
```

---

## 🎯 Полный процесс регистрации и покупки

### Шаг 1: Регистрация

1. **Пользователь заходит на сайт**
2. **Нажимает "Регистрация"**
3. **Вводит:**
   - Email
   - Пароль
   - Имя
4. **Получает email с подтверждением**
5. **Нажимает ссылку в письме → Аккаунт активирован**

### Шаг 2: Покупка пакета

1. **Авторизуется на сайте**
2. **Переходит в "💎 Пакеты единиц"**
3. **Выбирает пакет:**
   - Базовый: 500 ед. за 1 000₽
   - Профессиональный: 1 250 ед. за 2 500₽
4. **Нажимает "Купить"**
5. **Переход на страницу оплаты ЮKassa**
6. **Оплачивает картой/СБП/кошельком**
7. **После оплаты:**
   - Возврат на сайт
   - Баланс автоматически пополнен
   - Email с чеком и деталями

### Шаг 3: Использование

1. **Заходит в любой раздел (ЭКГ, МРТ и т.д.)**
2. **Делает анализ**
3. **Единицы списываются автоматически**
4. **Видит остаток в виджете баланса**

### Шаг 4: Повторная покупка

1. **Когда баланс заканчивается**
2. **Снова переходит в "💎 Пакеты единиц"**
3. **Покупает новый пакет**
4. **Баланс ПОПОЛНЯЕТСЯ** (не обнуляется)

---

## 📦 Необходимые сервисы

### 1. Хостинг базы данных
- **Supabase** (бесплатный тариф) ✅ Рекомендуется
- **Neon** (бесплатный PostgreSQL)
- **PlanetScale** (MySQL)

### 2. Email сервис
- **Resend** (бесплатно 3000 писем/месяц) ✅ Рекомендуется
- **SendGrid**
- **AWS SES**

### 3. Платежный шлюз
- **ЮKassa** (РФ) ✅ Рекомендуется для РФ
- **Stripe** (международные платежи)
- **Robokassa**

### 4. Аутентификация
- **Next-Auth** ✅ Рекомендуется
- **Supabase Auth**
- **Clerk**

---

## 💰 Примерные расходы

| Сервис | Бесплатный тариф | Платный |
|--------|------------------|---------|
| Supabase | 500 МБ БД, 2 GB transfer | $25/месяц |
| Resend | 3000 emails/месяц | $20/месяц за 50k |
| ЮKassa | 2.8% + 10₽ за транзакцию | |
| Vercel (хостинг) | Бесплатно | $20/месяц |

**Итого для старта: БЕСПЛАТНО** (в пределах лимитов)

---

## 🚀 Рекомендуемый стек (для быстрого старта)

```
Frontend:         Next.js 15 (уже есть) ✅
База данных:      Supabase PostgreSQL
Аутентификация:   Supabase Auth
Платежи:          ЮKassa
Email:            Resend
Хостинг:          Vercel
```

**Преимущества:**
- ✅ Все бесплатно для старта
- ✅ Легко интегрируется
- ✅ Автоматическое масштабирование
- ✅ Минимум кода

---

## 📝 Пример потока регистрации

### Email при регистрации:
```
От: noreply@yourdomain.com
Тема: Подтвердите регистрацию

Здравствуйте!

Спасибо за регистрацию в Медицинском ИИ-Ассистенте.

Подтвердите ваш email, нажав на ссылку:
[ПОДТВЕРДИТЬ EMAIL]

Ссылка действительна 24 часа.
```

### Email после покупки:
```
От: noreply@yourdomain.com
Тема: Пакет "Базовый" успешно активирован!

Здравствуйте!

Ваш платеж успешно обработан.

Детали покупки:
├─ Пакет: Базовый
├─ Единиц: 500
├─ Стоимость: 1 000₽
└─ Дата: 27 декабря 2025

Ваш текущий баланс: 500 единиц

[ПЕРЕЙТИ В ЛИЧНЫЙ КАБИНЕТ]

С уважением,
Команда Медицинского ИИ-Ассистента
```

---

## ⚙️ Статус системы подписки

```
🔴 ОТКЛЮЧЕНА (по вашему запросу)
```

Чтобы включить позже:
```bash
# В .env.local
NEXT_PUBLIC_SUBSCRIPTION_ENABLED=true
```

---

## 📊 Что уже готово

✅ Интерфейс страниц подписки  
✅ Логика управления балансом  
✅ Виджет баланса  
✅ История транзакций  
✅ Списание единиц  

## 🔜 Что нужно добавить

❌ Регистрация/авторизация (Next-Auth)  
❌ База данных PostgreSQL (Supabase)  
❌ Платежная интеграция (ЮKassa)  
❌ Email уведомления (Resend)  
❌ Связь userId с балансом  

---

## 💡 Рекомендация

**Для быстрого MVP используйте:**
1. **Supabase** - БД + Авторизация (все в одном)
2. **ЮKassa** - Платежи для РФ
3. **Resend** - Email уведомления

Это займет **2-3 дня** разработки для полноценной системы.

---

**Статус:** Подписка отключена, готова к интеграции с аутентификацией и платежами.





