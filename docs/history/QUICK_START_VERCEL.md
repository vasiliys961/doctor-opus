# 🚀 Быстрый старт - Деплой на Vercel

## Шаг 1: Установка зависимостей ✅

Зависимости уже установлены! Выполнено: `npm install`

## Шаг 2: Настройка переменных окружения

### Вариант A: Через Vercel Dashboard (рекомендуется)

1. Перейдите на https://vercel.com и войдите
2. Импортируйте ваш проект (GitHub/GitLab/Bitbucket)
3. В настройках проекта → **Environment Variables** добавьте:
   - `OPENROUTER_API_KEY` - ваш ключ от OpenRouter
   - `ASSEMBLYAI_API_KEY` - ваш ключ от AssemblyAI (опционально)

Подробная инструкция: см. `VERCEL_ENV_SETUP.md`

### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Добавьте переменные окружения
vercel env add OPENROUTER_API_KEY
vercel env add ASSEMBLYAI_API_KEY
```

## Шаг 3: Деплой

### Вариант A: Автоматический деплой (через GitHub)

1. Подключите репозиторий к Vercel
2. Vercel автоматически задеплоит при каждом push в main ветку

### Вариант B: Ручной деплой через CLI

```bash
# Деплой в продакшен
vercel --prod

# Или просто
vercel
```

## Шаг 4: Проверка

После деплоя:
1. Откройте URL вашего проекта (будет показан после деплоя)
2. Проверьте работу главной страницы
3. Попробуйте загрузить изображение для анализа
4. Проверьте логи в Vercel Dashboard → Functions → Logs

## Локальная разработка

```bash
# Запустите dev сервер
npm run dev

# Откройте http://localhost:3000
```

Для локальной разработки создайте `.env.local`:
```bash
OPENROUTER_API_KEY=sk-or-v1-your-key-here
ASSEMBLYAI_API_KEY=your-key-here
```

## Структура проекта

```
medical-assistant/
├── app/                    # Next.js страницы
│   ├── page.tsx           # Главная
│   ├── api/               # API endpoints
│   └── [pages]/           # Страницы анализа
├── components/            # React компоненты
├── api/                   # Python serverless functions
├── lib/                   # Общая логика
└── vercel.json           # Конфигурация Vercel
```

## Что дальше?

- ✅ Зависимости установлены
- ⏳ Настройте переменные окружения в Vercel
- ⏳ Задеплойте проект
- ⏳ Проверьте работу всех функций

## Проблемы?

См. `VERCEL_ENV_SETUP.md` для решения проблем с переменными окружения.

## Документация

- `MIGRATION_COMPLETE.md` - что было сделано
- `VERCEL_DEPLOY.md` - подробная инструкция по деплою
- `VERCEL_ENV_SETUP.md` - настройка переменных окружения

