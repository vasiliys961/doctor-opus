# Версия 3.33.0 (27 декабря 2025)

## 🆕 Что нового

### Обратная связь и самообучение
- **Компонент FeedbackForm**: Добавлена форма обратной связи для врачей во всех разделах анализа (ЭКГ, Рентген, МРТ, КТ, УЗИ, Дерматоскопия, Лаборатория, Генетика, Видео).
  - Оценка корректности (Полностью верно / Частично / Ошибка).
  - Выбор специальности врача.
  - Возможность указать правильный диагноз.
  - Сбор данных для дообучения моделей (dataset collection).
- **Backend-интеграция**:
  - Создан API-маршрут `/api/feedback` (Next.js).
  - Создан Python-обработчик `api/python/feedback/route.py`.
  - Реализовано сохранение отзывов в SQLite (`medical_data.db`) и дублирование в JSON-файлы (`logs/feedback_json/`) для удобства обучения.
- **Уникальные ID**: Введена система генерации `analysis_id` для точной привязки отзывов к конкретным сессиям анализа (включая стриминг через заголовки `X-Analysis-Id`).

## 🛠 Технические изменения

### Frontend
- Новый компонент `components/FeedbackForm.tsx`.
- Обновлены страницы: `app/ecg/page.tsx`, `app/xray/page.tsx`, `app/mri/page.tsx`, `app/ct/page.tsx`, `app/ultrasound/page.tsx`, `app/dermatoscopy/page.tsx`, `app/lab/page.tsx`, `app/genetic/page.tsx`, `app/image-analysis/page.tsx`, `app/video/page.tsx`.
- Настроен прокси-запрос к Python API для работы с базой данных.

### Backend
- Добавлен модуль `api/python/feedback/`.
- Обновлена логика сохранения в `utils/feedback_manager.py` (через вызовы из маршрута).
- Настроено автоматическое создание папки для JSON-логов.

## 📈 Преимущества
1. **Качество**: Позволяет собирать реальные ошибки моделей для последующего анализа и исправления промптов.
2. **Dataset**: Формируется база данных "Кейс -> Ответ модели -> Правильный ответ", что является основой для Fine-tuning.
3. **UX**: Врачи могут участвовать в улучшении системы, видя, что их мнение учитывается.




