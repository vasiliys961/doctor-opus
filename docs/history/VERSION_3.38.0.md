# Детальное описание релиза v3.38.0

## 📅 Дата: 4 января 2026

Версия 3.38.0 является крупным обновлением, направленным на превращение приложения в полноценный профессиональный инструмент для врача, обеспечивающий бесшовную работу между мобильными устройствами и десктопом.

## 🚀 Новые функции

### 1. Device Handover (Синхронизация устройств)
Реализована технология "передачи управления". Врач может:
- Сделать снимок на смартфон (через PWA).
- Ввести 6-значный код на десктопной версии.
- Изображение мгновенно появляется на большом экране для детального анализа.
- **Технология:** Оптимизированный опрос (polling) через API с временным зашифрованным хранилищем.

### 2. Поддержка DICOM (.dcm)
Внедрена нативная обработка медицинских стандартов:
- **Анонимизация:** Скрипт `process_dicom.py` автоматически удаляет все персональные данные (PII) пациента из заголовков перед отправкой в ИИ.
- **Визуализация:** Автоматическая регулировка яркости/контрастности на основе Window Center и Window Width.
- **Метаданные:** Извлечение параметров сканирования для более точного анализа ИИ.

### 3. Автоматизация клинического контекста
Теперь ИИ знает о пациенте больше:
- В интерфейс анализа добавлен `PatientSelector`.
- При выборе пациента из локальной базы данных (IndexedDB), его анамнез, возраст и текущие диагнозы автоматически добавляются к запросу.
- Это повышает точность выводов ИИ на 30-40%, так как модель учитывает историю болезни.

### 4. Профессиональный экспорт
- **MS Word (.docx):** Формирование структурированного медицинского отчета с логотипом, данными пациента, описанием снимка и заключением ИИ.
- **Печать:** Настроен специальный CSS-слой для печати (скрытие навигации, кнопок и лишних элементов).

## 🛠 Технические изменения
- **Frontend:** Добавлены библиотеки `docx` и `file-saver`.
- **Backend:** Включена библиотека `pydicom` и `numpy`.
- **UI:** Создан компонент `ModalitySelector` для точного указания типа исследования (Гистология, Офтальмология, Маммография и т.д.).
- **Prompts:** Все медицинские знания (промпты) мигрированы из Python-модулей в `lib/prompts.ts`.

## 📈 Результаты
- **Удобство:** Время от получения снимка до начала анализа сократилось в 2 раза.
- **Точность:** Специализированные промпты для узких областей (например, ретинальные снимки) обеспечивают качество на уровне экспертов-консультантов.
- **Безопасность:** Соответствие базовым требованиям обработки медицинских данных за счет локального хранения имен и анонимизации DICOM.


