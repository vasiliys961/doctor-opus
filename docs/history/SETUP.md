# ⚙️ Настройка проекта

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка API ключей

**Унифицированная система загрузки ключей для Streamlit и Replit**

Приложение автоматически определяет платформу и загружает ключи с правильным приоритетом.

**Приоритет загрузки:**
1. Переменные окружения (высший приоритет - особенно важно для Replit)
2. Streamlit secrets (UI) - если доступно
3. `.streamlit/secrets.toml` (локально для Streamlit)

**Вариант 1: Для локального запуска (Streamlit)**

Создайте файл `.streamlit/secrets.toml`:

```toml
[api_keys]
OPENROUTER_API_KEY = "sk-or-v1-ваш_ключ_здесь"
ASSEMBLYAI_API_KEY = "ваш_ключ_здесь"  # Опциональный

[medical_analyzer]
model_preference = "anthropic/claude-opus-4.5"
timeout = 180
max_retries = 3
```

**Вариант 2: Для Replit**

Используйте **Secrets** (переменные окружения):
- `OPENROUTER_API_KEY` - обязательный
- `ASSEMBLYAI_API_KEY` - опциональный (не ломает приложение)

Приложение автоматически определит платформу и загрузит ключи из переменных окружения.

**Важно:**
- **OPENROUTER_API_KEY** - обязательный для работы ИИ-функций и поиска протоколов
- **ASSEMBLYAI_API_KEY** - опциональный (функции транскрипции будут недоступны без него)

**Где получить ключи:**
- OpenRouter: https://openrouter.ai/keys
- AssemblyAI: https://www.assemblyai.com/app/account

**Примечание:** Поиск протоколов использует Claude Sonnet через OpenRouter, дополнительный API ключ не требуется.
- AssemblyAI: https://www.assemblyai.com/app/account

### 3. Запуск приложения

```bash
# Простой запуск
streamlit run app.py

# Или с проверками
python run_app.py
```

## Дополнительная информация о настройке

### Автоматическое определение платформы

Приложение автоматически определяет, на какой платформе оно запущено:
- **Replit** - определяется через переменную `REPL_ID`
- **Streamlit локально** - определяется через доступность `st.secrets`
- **Локально** - fallback режим

### Адаптивные настройки

В зависимости от платформы применяются разные настройки производительности:
- **Replit**: 25 MB максимальный размер загрузки, меньшие таймауты
- **Streamlit локально**: 50 MB максимальный размер загрузки
- **Локально**: 100 MB максимальный размер загрузки

Все настройки централизованы в классе `EnvironmentConfig` из `config.py`.

## Структура директорий

Проект автоматически создаст необходимые директории:
- `data/` - база данных
- `uploads/` - загруженные файлы
- `cache/` - кэш результатов
- `logs/` - логи приложения
- `exports/` - экспортированные данные
- `temp/` - временные файлы

## Проверка работы

1. Запустите приложение
2. Откройте раздел "🤖 ИИ-Консультант"
3. Нажмите "🔗 Тест подключения"
4. Должны появиться сообщения об успешном подключении ✅

## Устранение проблем

### Ошибка 401 (Unauthorized)
- Проверьте правильность API ключа
- Убедитесь, что ключ активен на OpenRouter.ai
- Проверьте баланс на https://openrouter.ai/credits

### Модули недоступны
- Проверьте импорты: `python -c "import streamlit; print('OK')"`
- Установите недостающие зависимости: `pip install -r requirements.txt`

### Ключи не загружаются
- Проверьте приоритет загрузки: переменные окружения > st.secrets > secrets.toml
- Для Replit: используйте Secrets (переменные окружения)
- Для Streamlit: проверьте наличие `.streamlit/secrets.toml` или используйте UI Secrets
- Проверьте переменные окружения: `echo $OPENROUTER_API_KEY`
- Проверьте логи приложения - они покажут, откуда загружены ключи

### Приложение не запускается на Replit
- Убедитесь, что ключи установлены через Secrets (переменные окружения)
- Проверьте файл `.replit` - он должен содержать команды запуска
- Приложение автоматически адаптируется под ограничения Replit

---
**Дополнительная информация:** См. `ARCHITECTURE.md` и `README.md`

