# 🔧 РЕШЕНИЕ ПРОБЛЕМЫ СО STREAMLIT CLOUD

## Проблема
Ошибка при установке зависимостей на Streamlit Cloud:
```
Error during processing dependencies! Please fix the error and push an update
```

## ✅ РЕШЕНИЕ

### Шаг 1: Используйте оптимизированный requirements.txt

Я создал обновленный `requirements.txt` с совместимыми версиями для Streamlit Cloud.

### Шаг 2: Проверьте файлы перед отправкой

Убедитесь, что в репозитории есть:
- ✅ `requirements.txt` (обновленный)
- ✅ `.streamlit/config.toml` (создан)
- ✅ `config.example.py` (шаблон)
- ❌ `config.py` (НЕ должен быть в git!)

### Шаг 3: Настройте секреты в Streamlit Cloud

1. Зайдите в настройки приложения на Streamlit Cloud
2. Перейдите в раздел "Secrets"
3. Добавьте секреты:

```toml
[api_keys]
OPENROUTER_API_KEY = "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"
ASSEMBLYAI_API_KEY = "dea6f5f506c2491588b8178de20c51a0"
```

### Шаг 4: Отправьте код в GitHub

```bash
git add requirements.txt .streamlit/config.toml config.example.py
git commit -m "Fix: Обновлены зависимости для Streamlit Cloud"
git push origin main
```

## 🔍 АЛЬТЕРНАТИВНОЕ РЕШЕНИЕ

Если проблема сохраняется, используйте минимальный набор зависимостей:

1. Переименуйте текущий `requirements.txt`:
   ```bash
   mv requirements.txt requirements-full.txt
   ```

2. Используйте минимальную версию:
   ```bash
   cp requirements-cloud.txt requirements.txt
   ```

3. Отправьте в GitHub:
   ```bash
   git add requirements.txt
   git commit -m "Fix: Минимальные зависимости для Streamlit Cloud"
   git push origin main
   ```

## 📋 ЧЕКЛИСТ ДЛЯ STREAMLIT CLOUD

- [ ] `requirements.txt` обновлен (без проблемных версий)
- [ ] `.streamlit/config.toml` создан
- [ ] `config.py` НЕ в git (проверьте `.gitignore`)
- [ ] Секреты добавлены в Streamlit Cloud
- [ ] Код отправлен в GitHub
- [ ] Приложение перезапущено на Streamlit Cloud

## 🐛 ЕСЛИ ПРОБЛЕМА СОХРАНЯЕТСЯ

1. Проверьте логи в Streamlit Cloud
2. Убедитесь, что Python версия 3.8-3.11 (не 3.12+)
3. Попробуйте удалить проблемные пакеты из requirements.txt:
   - `torch` и `torchvision` (очень большие, могут не установиться)
   - `opencv-python` (используйте `opencv-python-headless`)
   - `whisper` (большой пакет)

## 💡 РЕКОМЕНДАЦИИ

Для Streamlit Cloud лучше использовать минимальный набор зависимостей.
Тяжелые пакеты (torch, opencv) лучше загружать только при необходимости.

