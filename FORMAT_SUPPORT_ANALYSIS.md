# Анализ возможности добавления поддержки форматов

## ✅ Форматы, которые МОЖНО добавить без проблем:

### 1. **VCF.GZ** (сжатый VCF)
- **Библиотека**: Встроенный `zlib` в Node.js (не требует установки)
- **Сложность**: Низкая (1-2 часа)
- **Риск поломки**: Минимальный
- **Где добавить**: 
  - `lib/file-extractor.ts` - функция `extractVCFGZData()` уже есть, но возвращает ошибку
  - `app/api/extract/route.ts` - уже есть проверка на vcf.gz, но возвращает ошибку
- **Что нужно**: Реализовать распаковку через `zlib.gunzip()`, затем обработать как обычный VCF

### 2. **XLSX/XLS** (Excel файлы)
- **Библиотека**: `xlsx` (легкая, популярная) или `exceljs` (более функциональная)
- **Сложность**: Средняя (2-3 часа)
- **Риск поломки**: Низкий
- **Где добавить**: 
  - `lib/file-extractor.ts` - функция `extractExcelData()` уже есть, но возвращает ошибку
  - `app/api/extract/route.ts` - нужно добавить обработку
  - `app/api/analyze/lab/route.ts` - можно добавить поддержку
- **Что нужно**: 
  - Установить `xlsx`: `npm install xlsx`
  - Конвертировать Excel в CSV текст или JSON
  - Отправить в text API для анализа

### 3. **ZIP** (архивы)
- **Библиотека**: `adm-zip` (легкая) или встроенный модуль (для простых случаев)
- **Сложность**: Средняя (2-4 часа)
- **Риск поломки**: Средний (нужно обработать разные типы файлов внутри архива)
- **Где добавить**: 
  - `app/image-analysis/page.tsx` - упоминается в accept
  - `app/api/extract/route.ts` - можно добавить обработку
- **Что нужно**: 
  - Установить `adm-zip`: `npm install adm-zip`
  - Распаковать архив
  - Обработать каждый файл внутри (рекурсивно)
  - Объединить результаты

## ⚠️ Форматы, которые требуют дополнительных проверок:

### 4. **DCM/DICOM** (медицинские изображения)
- **Библиотека**: `dicom-parser` или `dcmjs` (специализированные для DICOM)
- **Сложность**: Высокая (4-6 часов)
- **Риск поломки**: Средний-высокий (DICOM сложный формат, нужна конвертация в обычное изображение)
- **Где добавить**: 
  - `app/image-analysis/page.tsx` - упоминается в accept
  - `app/api/analyze/image/route.ts` - нужно добавить обработку перед sharp
- **Что нужно**: 
  - Установить `dicom-parser`: `npm install dicom-parser`
  - Извлечь пиксельные данные из DICOM
  - Конвертировать в PNG/JPG через sharp
  - Обработать как обычное изображение
- **Особенности**: 
  - DICOM файлы могут быть очень большими
  - Нужна обработка метаданных (пациент, исследование и т.д.)
  - Может потребоваться обработка разных типов данных (16-bit, grayscale и т.д.)

## 📋 Итоговая оценка:

| Формат | Сложность | Риск | Время | Рекомендация |
|--------|-----------|------|-------|--------------|
| VCF.GZ | Низкая | Минимальный | 1-2ч | ✅ Добавить |
| XLSX/XLS | Средняя | Низкий | 2-3ч | ✅ Добавить |
| ZIP | Средняя | Средний | 2-4ч | ⚠️ Добавить с осторожностью |
| DCM/DICOM | Высокая | Средний-высокий | 4-6ч | ⚠️ Требует обсуждения |

## 🔍 Проверка совместимости:

✅ **Не сломает существующий код**, потому что:
- Все форматы будут обрабатываться в отдельных функциях
- Существующие форматы (JPG, PNG, PDF, CSV, TXT, VCF) продолжат работать как раньше
- Новые форматы будут добавляться через расширение `detectFileType()` и `extractFileData()`
- Ошибки будут обрабатываться gracefully (fallback на существующую логику)

## 📦 Необходимые библиотеки:

```bash
# Обязательные (легкие):
npm install xlsx          # Для Excel (XLSX/XLS)
npm install adm-zip       # Для ZIP архивов

# Опциональные (для DICOM):
npm install dicom-parser  # Для DICOM файлов (если решим добавить)
```

## 🎯 Рекомендация:

**Можно безопасно добавить:**
1. ✅ **VCF.GZ** - самый простой, уже есть заготовка
2. ✅ **XLSX/XLS** - часто используется, есть заготовка
3. ⚠️ **ZIP** - полезно, но нужно продумать обработку вложенных файлов
4. ⚠️ **DICOM** - сложный формат, лучше обсудить необходимость



