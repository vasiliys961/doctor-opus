# 📋 ИНСТРУКЦИЯ: Автоматическая система анализа feedback

## ✅ Простые ответы на ваши вопросы:

### 1. Где сейчас ваши данные?
**✅ Локально на вашем MacBook:**
- `medical_data.db` - база данных со всеми отзывами
- `data/raw_feedback/` - JSON файлы с сырыми отзывами

### 2. Можно ли включить анализ?
**✅ ДА!** Система работает автоматически при запуске приложения.

### 3. Автоматизация процесса?
**✅ ДА!** Реализовано:
- После **10 тестов** по каждому исследованию → автоматический анализ
- После **25 анализов** → предложения по оптимизации промптов
- После **1000 тестов** → предложение скачать с GitHub и проанализировать

### 4. Объем данных:

| Количество | Размер (МБ) | Сожрут ли память? |
|------------|-------------|-------------------|
| **1000 тестов** | ~8-18 МБ | ❌ НЕТ (это как 1-2 песни) |
| **10000 тестов** | ~80-180 МБ | ❌ НЕТ (это как 2-3 минуты видео) |

**Вывод:** Даже 10000 тестов = ~180 МБ максимум (не критично!)

### 5. GitHub и облако:

| Количество | Размер на GitHub | Обвалит ли? |
|------------|------------------|-------------|
| **1000 тестов** | ~1-3 МБ (анонимизированных) | ❌ НЕТ |
| **10000 тестов** | ~10-30 МБ (анонимизированных) | ❌ НЕТ |

**GitHub лимит:** до 1 ГБ на репозиторий (бесплатно)  
**10000 тестов** = ~30 МБ (в 33 раза меньше лимита!)

### 6. Где база на GitHub?
**✅ Путь:** `data/anonymized_cases/*.jsonl` в вашем репозитории

Доступна с любого устройства через:
```bash
git pull
# Или скачать напрямую с GitHub через веб-интерфейс
```

### 7. Автоматическое предложение после 1000 тестов?
**✅ ДА!** При запуске приложения система автоматически предложит скачать и проанализировать.

---

## 🔄 Как это работает (автоматически):

### При запуске приложения:

1. **Проверка:** Есть ли ≥10 отзывов по типу?
   - **ДА** → Показывается уведомление: "📊 Накоплено X типов анализов с ≥10 отзывами"
   - **Кнопка:** "🔍 Проанализировать..."
   - **Результат:** Показывается топ ошибок для этого типа

2. **Проверка:** Есть ли ≥25 отзывов общих?
   - **ДА** → Показывается уведомление: "✅ Накоплено 25+ отзывов. Можно сформировать предложения"
   - **Кнопка:** "🎯 Получить предложения по оптимизации"
   - **Результат:** Показывается отчет с предложениями по оптимизации промптов

3. **Проверка:** Есть ли ≥1000 отзывов?
   - **ДА** → Показывается уведомление: "🎯 Накоплено 1000+ отзывов! Рекомендуется скачать с GitHub"
   - **Кнопка:** "📥 Скачать с GitHub и проанализировать"
   - **Результат:** Инструкции для глубокого анализа

---

## 📋 Порядок действий (всё автоматически):

### Этап 1: Накопление данных (автоматически)
- Врачи оставляют отзывы через форму в приложении
- Данные автоматически сохраняются в `medical_data.db`
- Если есть согласие → сохраняются в `data/raw_feedback/`

### Этап 2: Автоматический анализ (после 10 тестов)
**Что происходит:**
- При запуске приложения система проверяет количество отзывов
- Если ≥10 по типу → показывается уведомление и кнопка
- Вы нажимаете кнопку → запускается анализ
- Результат: показывается топ ошибок, частые проблемы

**Где увидеть:**
- Вверху страницы при запуске приложения
- Уведомление с кнопкой "Проанализировать"

### Этап 3: Предложения по оптимизации (после 25 анализов)
**Что происходит:**
- Система анализирует все накопленные отзывы
- Ищет паттерны ошибок
- Формирует конкретные предложения по улучшению промптов
- Показывает отчет с рекомендациями

**Результат:**
- Отчет в текстовом поле
- Список топ ошибок по каждому типу
- Конкретные предложения: "Добавьте в промпт упоминание о..."

### Этап 4: Массовый анализ (после 1000 тестов)
**Что происходит:**
- Система проверяет количество на GitHub или локально
- Предлагает скачать данные
- Показывает инструкции для глубокого анализа

**Действия:**
1. Нажать "📥 Скачать с GitHub"
2. Выполнить: `git pull` (или скачать через веб)
3. Запустить: `python scripts/get_feedback_data.py --export json`
4. Запустить глубокий анализ

---

## 🎯 Практические примеры:

### Пример 1: У вас 15 отзывов по ЭКГ
**При запуске приложения:**
```
📊 ✅ Накоплено 1 типов анализов с ≥10 отзывами. 
Рекомендуется провести анализ.

[🔍 Проанализировать ECG...]
```

**После нажатия:**
```
✅ Анализ завершен: 15 отзывов

⚠️ Топ ошибок:
1. Фибрилляция предсердий (вместо синусового ритма)
2. Блокада левой ножки пучка Гиса (не распознана)
3. Желудочковая экстрасистолия (пропущена)
```

### Пример 2: У вас 30 отзывов общих
**При запуске приложения:**
```
💡 ✅ Накоплено 30 отзывов. 
Можно сформировать предложения по оптимизации промптов.

[🎯 Получить предложения по оптимизации]
```

**После нажатия:**
```
📋 Отчет по оптимизации:

🔍 ECG
   Всего отзывов: 15
   Процент ошибок: 13.3%
   📝 Предложения по оптимизации:
      • Для ECG обнаружены частые ошибки. 
        Рекомендуется добавить в промпт упоминание о: 
        Фибрилляция предсердий, Блокада левой ножки пучка Гиса
```

### Пример 3: У вас 1200 отзывов
**При запуске приложения:**
```
🎯 Накоплено 1200 отзывов! 
Рекомендуется скачать данные с GitHub и провести глубокий анализ локально.

[📥 Скачать с GitHub и проанализировать]
```

**После нажатия:**
```
💡 Используйте: 
git pull для скачивания данных с GitHub, 
затем python scripts/get_feedback_data.py --export json
```

---

## 📊 Объемы данных (детально):

### Расчет для 1 отзыва:
- **База данных:** ~5-10 КБ
- **JSON файл:** ~2-5 КБ  
- **Анонимизированный:** ~1-3 КБ

### Расчеты по объемам:

| Количество | БД (МБ) | JSON (МБ) | Аноним (МБ) | ИТОГО (МБ) |
|------------|---------|-----------|-------------|------------|
| 100 тестов | 0.5-1   | 0.2-0.5   | 0.1-0.3     | ~1-2 МБ    |
| 1000 тестов| 5-10    | 2-5       | 1-3         | ~8-18 МБ   |
| 10000 тестов| 50-100  | 20-50     | 10-30       | ~80-180 МБ |

**Вывод:** Даже 10000 тестов = ~180 МБ (это примерно 2-3 минуты видео)

---

## ☁️ GitHub и синхронизация:

### Где лежит база на GitHub?
**Путь в репозитории:**
```
ваш-репозиторий/
  └── data/
      └── anonymized_cases/
          ├── cases_20251207.jsonl  ← здесь
          ├── cases_20251208.jsonl
          └── cases_20251209.jsonl
```

### Как синхронизировать?

**Вариант 1: Автоматический (скрипт)**
```bash
./scripts/sync_to_github.sh
```

**Вариант 2: Ручной**
```bash
# 1. Анонимизировать новые отзывы
python -c "from feedback.anonymizer import MedicalAnonymizer; ..."

# 2. Добавить в git
git add data/anonymized_cases/*.jsonl

# 3. Закоммитить
git commit -m "Add anonymized feedback cases"

# 4. Запушить
git push
```

### Как получить с другого устройства?
```bash
git pull
# Или
git clone <ваш-репозиторий>
cd <репозиторий>
# Данные будут в data/anonymized_cases/
```

---

## 🔍 Как получить данные для анализа:

### Вариант 1: Через автоматическую систему (рекомендуется)
- Просто запустите приложение
- Система автоматически предложит анализ при достижении порогов

### Вариант 2: Через скрипт
```bash
# Показать статистику
python scripts/get_feedback_data.py --stats

# Экспорт в JSON
python scripts/get_feedback_data.py --export json

# Экспорт в CSV
python scripts/get_feedback_data.py --export csv
```

### Вариант 3: Напрямую из базы
```bash
sqlite3 medical_data.db
SELECT * FROM analysis_feedback;
```

---

## ✅ Чек-лист действий:

- [ ] Данные накапливаются автоматически при использовании формы обратной связи
- [ ] При запуске приложения проверьте уведомления вверху страницы
- [ ] После 10 отзывов по типу → нажмите "Проанализировать"
- [ ] После 25 отзывов общих → нажмите "Получить предложения"
- [ ] Периодически (раз в неделю/месяц) → запускайте `./scripts/sync_to_github.sh`
- [ ] После 1000 отзывов → скачайте с GitHub и проведите глубокий анализ

---

## 🚀 Всё готово!

Система работает **полностью автоматически**. Вам нужно:
1. Использовать приложение (врачи оставляют отзывы)
2. При запуске проверять уведомления вверху страницы
3. Нажимать кнопки когда система предложит

**Всё остальное - автоматически!** 🎉

---

**Версия:** 1.0  
**Дата:** 2025-12-07  
**Статус:** ✅ Готово к использованию


