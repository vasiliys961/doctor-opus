# Руководство по работе с данными обратной связи

## 📍 Где накапливаются данные?

Данные сохраняются в **двух местах одновременно**:

### 1. База данных SQLite (`medical_data.db`)
- **Таблица:** `analysis_feedback`
- **Местоположение:** В корне проекта
- **Содержит:** Все отзывы со всеми полями (специальность, оценка, комментарии, правильный диагноз)
- **Назначение:** Быстрый доступ, статистика, поиск

### 2. JSON файлы (`data/raw_feedback/`)
- **Формат:** `feedback_YYYYMMDD_HHMMSS_тип.json`
- **Местоположение:** `data/raw_feedback/`
- **Содержит:** Сырые отзывы с ПИ (персональными данными)
- **Назначение:** Подготовка к анонимизации и обучению
- **⚠️ ВАЖНО:** Эти файлы НЕ коммитятся в git (добавлены в `.gitignore`)

---

## 🔄 Процесс накопления данных

```
1. Врач оставляет отзыв через форму
   ↓
2. Данные сохраняются в БД (medical_data.db)
   ↓
3. Если есть согласие → сохраняются в JSON (data/raw_feedback/)
   ↓
4. Периодически запускается анонимизация
   ↓
5. Анонимизированные данные → data/anonymized_cases/*.jsonl
   ↓
6. Синхронизация с GitHub (опционально)
   ↓
7. Подготовка данных для обучения → data/training_data/*.jsonl
```

---

## 📥 Как получить данные?

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Показать статистику и последние 10 отзывов
python scripts/get_feedback_data.py

# Показать только статистику
python scripts/get_feedback_data.py --stats

# Фильтр по типу анализа
python scripts/get_feedback_data.py --type ECG

# Экспорт в CSV
python scripts/get_feedback_data.py --export csv

# Экспорт в JSON
python scripts/get_feedback_data.py --export json

# Указать путь к файлу
python scripts/get_feedback_data.py --export json --output my_feedback.json
```

### Вариант 2: Через Python код

```python
from utils.feedback_manager import get_feedback_stats
from feedback.analyzer import FeedbackAnalyzer
import sqlite3

# Статистика
stats = get_feedback_stats()
print(f"Всего отзывов: {stats['total']}")

# Анализ
analyzer = FeedbackAnalyzer()
analysis = analyzer.analyze_feedback_patterns("ECG")

# Прямой доступ к БД
conn = sqlite3.connect('medical_data.db')
cursor = conn.cursor()
cursor.execute("SELECT * FROM analysis_feedback WHERE analysis_type = 'ECG'")
rows = cursor.fetchall()
conn.close()
```

### Вариант 3: Через SQL запросы

```bash
# Прямой доступ к SQLite
sqlite3 medical_data.db

# В SQLite консоли:
SELECT * FROM analysis_feedback;
SELECT COUNT(*) FROM analysis_feedback WHERE analysis_type = 'ECG';
SELECT * FROM analysis_feedback ORDER BY created_at DESC LIMIT 10;
```

---

## ☁️ Синхронизация с GitHub

### Автоматическая синхронизация

```bash
# Запустить скрипт синхронизации
./scripts/sync_to_github.sh
```

**Что делает скрипт:**
1. Анонимизирует сырые отзывы из `data/raw_feedback/`
2. Сохраняет в `data/anonymized_cases/cases_YYYYMMDD.jsonl`
3. Коммитит в git
4. Пушит на GitHub

### Ручная синхронизация

```bash
# 1. Анонимизация
python -c "
from feedback.anonymizer import MedicalAnonymizer
from pathlib import Path
from datetime import datetime

anonymizer = MedicalAnonymizer()
raw_dir = Path('data/raw_feedback')
output_file = Path(f'data/anonymized_cases/cases_{datetime.now().strftime(\"%Y%m%d\")}.jsonl')

if raw_dir.exists() and len(list(raw_dir.glob('*.json'))) > 0:
    output_file.parent.mkdir(parents=True, exist_ok=True)
    count = anonymizer.batch_process(raw_dir, output_file)
    print(f'✅ Обработано {count} кейсов')
"

# 2. Git операции
git add data/anonymized_cases/*.jsonl
git commit -m "Add anonymized feedback cases"
git push
```

---

## 📊 Структура данных

### В базе данных (`analysis_feedback`):

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | INTEGER | Уникальный ID |
| `analysis_type` | TEXT | Тип анализа (ECG, XRAY, MRI, etc.) |
| `analysis_id` | TEXT | ID конкретного анализа |
| `ai_response` | TEXT | Ответ ИИ (до 5000 символов) |
| `feedback_type` | TEXT | Тип обратной связи |
| `doctor_comment` | TEXT | Комментарий врача |
| `correct_diagnosis` | TEXT | Правильный диагноз |
| `specialty` | TEXT | Специальность врача |
| `correctness` | TEXT | Оценка корректности |
| `consent` | INTEGER | Согласие (0/1) |
| `input_case` | TEXT | Входной кейс |
| `created_at` | TIMESTAMP | Дата/время создания |

### В JSON файлах (`data/raw_feedback/`):

```json
{
  "timestamp": "2025-12-07T15:30:00",
  "analysis_type": "ECG",
  "input_case": "ЭКГ: ЧСС=75...",
  "model_output": "Диагноз: ...",
  "correctness": "⚠️ Частично верно",
  "correct_answer": "Правильный диагноз: ...",
  "specialty": "Кардиология",
  "comment": "Комментарий врача",
  "consent": true
}
```

### В анонимизированных файлах (`data/anonymized_cases/`):

```json
{
  "case_id": "uuid",
  "created_at": "2025-12-07T15:35:00",
  "analysis_type": "ECG",
  "input": "[ПИ УДАЛЕНО] ЭКГ: ЧСС=75...",
  "model_output": "...",
  "correctness": "⚠️ Частично верно",
  "correct_answer": "[ПИ УДАЛЕНО] ...",
  "specialty": "Кардиология",
  "comment": "[ПИ УДАЛЕНО] ...",
  "anonymization_applied": true
}
```

---

## 🔍 Как получить данные с GitHub?

### Вариант 1: Через git clone/pull

```bash
# Если у вас есть доступ к репозиторию
git clone <ваш-репозиторий>
cd <репозиторий>
git pull

# Данные будут в data/anonymized_cases/
```

### Вариант 2: Через GitHub API (если нужно)

```python
import requests

# Получить файлы из репозитория
# (требует токен доступа)
```

### Вариант 3: Скачать напрямую с GitHub

1. Зайдите на GitHub в ваш репозиторий
2. Перейдите в `data/anonymized_cases/`
3. Скачайте нужные `.jsonl` файлы

---

## 📈 Мониторинг накопления данных

### Проверка статистики

```bash
# Через скрипт
python scripts/get_feedback_data.py --stats

# Через Python
python -c "
from feedback.storage import get_feedback_stats
stats = get_feedback_stats()
print(f'Сырых отзывов: {stats[\"raw_feedback_count\"]}')
print(f'Анонимизированных: {stats[\"anonymized_cases_count\"]}')
print(f'Готовых для обучения: {stats[\"training_data_count\"]}')
"
```

### Проверка размера базы данных

```bash
ls -lh medical_data.db
du -sh data/raw_feedback/
du -sh data/anonymized_cases/
```

---

## 🎯 Рекомендуемый workflow

### Ежедневно/еженедельно:

1. **Проверка накопления:**
   ```bash
   python scripts/get_feedback_data.py --stats
   ```

2. **Анонимизация (когда накопится 10+ отзывов):**
   ```bash
   python -c "from feedback.anonymizer import MedicalAnonymizer; ..."
   ```

3. **Синхронизация с GitHub (по необходимости):**
   ```bash
   ./scripts/sync_to_github.sh
   ```

### Перед дообучением:

1. **Подготовка данных:**
   ```bash
   python training/prepare_training_data.py
   ```

2. **Проверка количества:**
   ```bash
   wc -l data/training_data/flash_sft.jsonl
   ```

3. **Дообучение (если достаточно данных):**
   ```bash
   python training/lora_finetune.py
   ```

---

## 🔐 Безопасность

- ✅ **Сырые данные** (`data/raw_feedback/`) - НЕ коммитятся в git
- ✅ **Анонимизированные данные** (`data/anonymized_cases/`) - можно коммитить
- ✅ **База данных** (`medical_data.db`) - НЕ коммитится (в `.gitignore`)

---

## ❓ Частые вопросы

**Q: Где находятся данные прямо сейчас?**  
A: В `medical_data.db` (БД) и `data/raw_feedback/` (JSON файлы)

**Q: Как получить все данные для анализа?**  
A: `python scripts/get_feedback_data.py --export json`

**Q: Данные на GitHub?**  
A: Только анонимизированные (после запуска `sync_to_github.sh`)

**Q: Можно ли получить данные с другого компьютера?**  
A: Да, через `git pull` или скачав файлы с GitHub

**Q: Как часто синхронизировать с GitHub?**  
A: По мере накопления (раз в неделю/месяц, или когда накопится 20+ кейсов)

---

---

## 🤖 Автоматическая система анализа (НОВОЕ!)

### Как это работает:

**При каждом запуске приложения** система автоматически проверяет:
- Есть ли ≥10 отзывов по типу анализа? → Предлагает базовый анализ
- Есть ли ≥25 отзывов общих? → Предлагает оптимизацию промптов  
- Есть ли ≥1000 отзывов? → Предлагает скачать с GitHub и провести глубокий анализ

**Вы увидите уведомления вверху страницы** при запуске приложения!

### Порядок действий (автоматический):

1. **10+ отзывов** → Нажмите "Проанализировать" → Увидите топ ошибок
2. **25+ отзывов** → Нажмите "Получить предложения" → Увидите отчет по оптимизации
3. **1000+ отзывов** → Нажмите "Скачать с GitHub" → Получите инструкции для глубокого анализа

**Всё автоматически!** Вам нужно только нажать кнопку когда система предложит.

---

**Версия:** 2.0  
**Дата:** 2025-12-07

