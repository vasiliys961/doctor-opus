# 📤 ОТПРАВКА ПРИЛОЖЕНИЯ В GITHUB

## Инструкция по отправке кода в репозиторий

Ваш репозиторий: `https://github.com/vasiliys961/medical-assistant33.git`

---

## 🚀 БЫСТРАЯ ОТПРАВКА (все команды подряд)

```bash
# 1. Перейдите в папку проекта
cd "/Users/maxmobiles.ru/Desktop/medical-assistant3 Р"

# 2. Инициализируйте git (если еще не сделано)
git init

# 3. Добавьте удаленный репозиторий
git remote add origin https://github.com/vasiliys961/medical-assistant33.git

# 4. Добавьте все файлы
git add .

# 5. Создайте первый коммит
git commit -m "Initial commit: Медицинский ИИ-Ассистент v6.0"

# 6. Отправьте в GitHub
git push -u origin main
```

Если ветка называется `master` вместо `main`:
```bash
git push -u origin master
```

---

## 📝 ПОДРОБНАЯ ИНСТРУКЦИЯ

### Шаг 1: Проверка git

```bash
cd "/Users/maxmobiles.ru/Desktop/medical-assistant3 Р"
git status
```

Если git не инициализирован, выполните:
```bash
git init
```

### Шаг 2: Настройка git (если первый раз)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### Шаг 3: Добавление удаленного репозитория

```bash
# Проверьте, есть ли уже origin
git remote -v

# Если нет, добавьте
git remote add origin https://github.com/vasiliys961/medical-assistant33.git

# Если уже есть, обновите URL
git remote set-url origin https://github.com/vasiliys961/medical-assistant33.git
```

### Шаг 4: Добавление файлов

```bash
# Добавить все файлы
git add .

# Или добавить конкретные файлы
git add app.py claude_assistant.py config.py
git add modules/ utils/ storages/
```

### Шаг 5: Создание коммита

```bash
git commit -m "Initial commit: Медицинский ИИ-Ассистент v6.0

- Анализ ЭКГ, рентгена, МРТ, КТ, УЗИ
- Генерация протоколов осмотра
- Сканирование медицинских документов
- Клинический контекст пациента
- База данных пациентов
- ИИ-консультант"
```

### Шаг 6: Отправка в GitHub

```bash
# Если репозиторий пустой (первая отправка)
git push -u origin main

# Если ветка называется master
git push -u origin master

# Если возникнут конфликты
git push -u origin main --force
```

---

## ⚠️ ВАЖНО: Что НЕ отправлять

Файл `.gitignore` уже настроен, чтобы НЕ отправлять:

- ❌ `config.py` - файл с API ключами
- ❌ `*.db` - базы данных
- ❌ `venv/` - виртуальное окружение
- ❌ `cache/` - кэш
- ❌ `logs/` - логи
- ❌ `generated_documents/` - сгенерированные документы
- ❌ `uploads/` - загруженные файлы

### Создайте шаблон config.py для GitHub

Создайте файл `config.example.py`:

```python
# config.example.py
# Скопируйте этот файл в config.py и добавьте свои API ключи

OPENROUTER_API_KEY = "ваш_ключ_здесь"
ASSEMBLYAI_API_KEY = "ваш_ключ_здесь"

# Другие настройки...
```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/vasiliys961/medical-assistant33.git
```

### Проблема: "failed to push some refs"
```bash
# Сначала получите изменения
git pull origin main --allow-unrelated-histories

# Затем отправьте
git push -u origin main
```

### Проблема: Требуется аутентификация
```bash
# Используйте Personal Access Token вместо пароля
# Или настройте SSH ключи
```

### Проблема: "branch 'main' does not exist"
```bash
# Создайте ветку main
git branch -M main
git push -u origin main
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД ОТПРАВКОЙ

- [ ] Проверен `.gitignore` - API ключи не попадут в репозиторий
- [ ] Создан `config.example.py` с шаблоном
- [ ] Все файлы добавлены: `git add .`
- [ ] Создан коммит: `git commit -m "..."`
- [ ] Репозиторий подключен: `git remote add origin ...`
- [ ] Код отправлен: `git push -u origin main`

---

## 🎯 БЫСТРАЯ КОМАНДА (скопируйте и выполните)

```bash
cd "/Users/maxmobiles.ru/Desktop/medical-assistant3 Р" && \
git init && \
git remote add origin https://github.com/vasiliys961/medical-assistant33.git 2>/dev/null || git remote set-url origin https://github.com/vasiliys961/medical-assistant33.git && \
git add . && \
git commit -m "Initial commit: Медицинский ИИ-Ассистент v6.0" && \
git branch -M main && \
git push -u origin main
```

---

## ✅ ПОСЛЕ ОТПРАВКИ

Проверьте репозиторий:
https://github.com/vasiliys961/medical-assistant33

Все файлы должны быть там!

