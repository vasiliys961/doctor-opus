# 📊 Как узнать затраты токенов

## 🎯 Быстрый способ (рекомендуется)

### Через веб-интерфейс:
1. Откройте приложение в браузере
2. Перейдите на страницу **`/statistics`** (или `/app/statistics`)
3. На странице отобразится:
   - Общая статистика за текущий месяц
   - Затраты по разделам (ЭКГ, МРТ, чат и т.д.)
   - Затраты по моделям
   - Количество запросов

### Где хранится статистика:
- **localStorage браузера** (ключ `usageBySections`)
- Автоматически очищается при смене месяца
- Доступна только в том браузере, где вы работали

---

## 🔍 Через консоль браузера

Откройте консоль разработчика (F12) и выполните:

```javascript
// Получить статистику по разделам
const data = JSON.parse(localStorage.getItem('usageBySections') || '{}');
console.log('Статистика по разделам:', data);

// Подсчитать общую стоимость
let totalCost = 0;
Object.values(data).forEach(section => {
  totalCost += section.costUnits || 0;
});
console.log('Общая стоимость (у.е.):', totalCost.toFixed(2));
console.log('Общая стоимость (USD):', (totalCost / 100).toFixed(4));

// Детальная статистика
Object.entries(data).forEach(([section, stats]) => {
  console.log(`\n${stats.sectionName}:`);
  console.log(`  Запросов: ${stats.calls}`);
  console.log(`  Стоимость: ${stats.costUnits.toFixed(2)} у.е. ($${(stats.costUnits / 100).toFixed(4)} USD)`);
  console.log(`  Модели:`, stats.models);
});
```

---

## 📝 Через логи (ограниченно)

Логи содержат информацию о токенах, но не полную статистику:

```bash
# Просмотр последних записей о токенах
tail -100 logs/medical_assistant.log | grep -i "токен\|token\|стоимость\|cost"

# Или используйте скрипт
node scripts/check_token_usage.js
```

**Примечание:** Логи могут не содержать полной информации, так как основная статистика хранится в браузере.

---

## 💰 Формула расчета стоимости

### Условные единицы (у.е.):
```
у.е. = (стоимость в USD) × 100
```

### Стоимость в USD:
```
USD = (входные_токены / 1,000,000) × цена_входа + (выходные_токены / 1,000,000) × цена_выхода
```

### Цены моделей (USD за 1M токенов):

| Модель | Входные | Выходные |
|--------|---------|----------|
| Claude Opus 4.5 | $15.00 | $75.00 |
| Claude Sonnet 4.5 | $3.00 | $15.00 |
| Claude Haiku 4.5 | $1.00 | $5.00 |
| Gemini 2.5 Flash | $0.30 | $2.50 |
| Gemini 3 Flash | $0.50 | $3.00 |
| Gemini 3 Pro | $1.25 | $5.00 |

---

## 📊 Примеры затрат

### Быстрый анализ (Gemini Flash):
- ~3,550 токенов
- **~$0.006 USD** (~0.60 ₽)

### Точный анализ (Opus 4.5):
- ~3,550 токенов
- **~$0.20 USD** (~20 ₽)

### Анализ с валидацией:
- ~5,150 токенов
- **~$0.25 USD** (~25 ₽)

---

## ⚠️ Важные замечания

1. **Статистика хранится локально** в браузере
2. **Очищается автоматически** при смене месяца
3. **Не синхронизируется** между устройствами/браузерами
4. **Для точных данных** используйте страницу `/statistics`

---

## 🔧 Сброс статистики

### Очистить текущий месяц:
```javascript
localStorage.removeItem('usageBySections');
```

### Или через интерфейс:
- Откройте `/statistics`
- Нажмите кнопку "Очистить статистику месяца"

---

## 📞 Дополнительная информация

- Полный анализ стоимости: `COST_ANALYSIS.md`
- Страница статистики: `app/statistics/page.tsx`
- Логирование: `lib/simple-logger.ts`
- Калькулятор стоимости: `lib/cost-calculator.ts`





