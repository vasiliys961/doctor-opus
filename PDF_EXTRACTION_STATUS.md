# Статус поддержки PDF извлечения по разделам

## ✅ Разделы С механизмами извлечения PDF:

1. **Лабораторный анализ** (`app/lab/page.tsx`)
   - ✅ Принимает: PDF, XLSX, XLS, CSV, изображения
   - ✅ Механизм: Конвертация PDF → изображения → Vision API (OCR)
   - ✅ API: `/api/analyze/lab` с поддержкой изображений

2. **Генетический анализ** (`app/genetic/page.tsx`)
   - ✅ Принимает: PDF, VCF, изображения
   - ✅ Механизм: Конвертация PDF → изображения → Vision API (OCR)
   - ✅ API: `/api/analyze/genetic` с поддержкой изображений

3. **Сканирование документов** (`app/document/page.tsx`)
   - ✅ Принимает: PDF, изображения
   - ✅ Механизм: Конвертация PDF → изображения → Vision API (OCR)
   - ✅ API: `/api/analyze/image` с поддержкой изображений

## ❌ Разделы БЕЗ механизмов извлечения PDF:

1. **Универсальный анализ изображений** (`app/image-analysis/page.tsx`)
   - ⚠️ Принимает: PDF (в accept), изображения, DICOM, ZIP, CSV, VCF, TXT
   - ❌ НЕТ механизма: Отправляет PDF в `/api/analyze/image`, который не обрабатывает PDF
   - ❌ Проблема: PDF файлы будут обработаны как изображения через sharp, что не сработает

2. **ЭКГ анализ** (`app/ecg/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

3. **Рентген анализ** (`app/xray/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

4. **МРТ анализ** (`app/mri/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

5. **КТ анализ** (`app/ct/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

6. **УЗИ анализ** (`app/ultrasound/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

7. **Дерматоскопия** (`app/dermatoscopy/page.tsx`)
   - ❌ Принимает: Только изображения (`image/*`)
   - ❌ НЕТ поддержки PDF

8. **Видео анализ** (`app/video/page.tsx`)
   - ❌ Принимает: Только видео файлы
   - ❌ НЕТ поддержки PDF

## ⚠️ API endpoints с частичной поддержкой:

1. **`/api/extract`** (`app/api/extract/route.ts`)
   - ⚠️ Упоминает PDF в комментариях
   - ❌ НЕТ механизма: Перенаправляет на `/api/scan/document`
   - ❌ Проблема: `/api/scan/document` работает только с изображениями

2. **`/api/scan/document`** (`app/api/scan/document/route.ts`)
   - ⚠️ Работает только с изображениями
   - ❌ НЕТ механизма конвертации PDF → изображения
   - ❌ Проблема: Не может обработать PDF напрямую

3. **`/api/analyze/image`** (`app/api/analyze/image/route.ts`)
   - ⚠️ Обрабатывает изображения
   - ❌ НЕТ механизма конвертации PDF → изображения
   - ❌ Проблема: PDF файлы будут обработаны через sharp, что не сработает

## 📋 Итоговый список разделов БЕЗ механизмов извлечения PDF:

1. **Универсальный анализ изображений** (`app/image-analysis/page.tsx`) - ⚠️ КРИТИЧНО (принимает PDF, но не обрабатывает)
2. **ЭКГ анализ** (`app/ecg/page.tsx`)
3. **Рентген анализ** (`app/xray/page.tsx`)
4. **МРТ анализ** (`app/mri/page.tsx`)
5. **КТ анализ** (`app/ct/page.tsx`)
6. **УЗИ анализ** (`app/ultrasound/page.tsx`)
7. **Дерматоскопия** (`app/dermatoscopy/page.tsx`)
8. **Видео анализ** (`app/video/page.tsx`)
9. **API `/api/extract`** (`app/api/extract/route.ts`)
10. **API `/api/scan/document`** (`app/api/scan/document/route.ts`)



