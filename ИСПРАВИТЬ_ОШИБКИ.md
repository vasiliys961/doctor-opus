# 🔴 ИСПРАВЛЕНИЕ КРАСНЫХ ОШИБОК

## Быстрое исправление

### Шаг 1: Запустите диагностику
```bash
python diagnose_errors.py
```

Это покажет все ошибки и предупреждения.

### Шаг 2: Автоматическое исправление
```bash
python fix_errors.py
```

Это установит все недостающие пакеты.

### Шаг 3: Установите все зависимости
```bash
source venv/bin/activate
pip install -r requirements.txt
```

---

## Частые красные ошибки и их исправление

### ❌ Ошибка: "ModuleNotFoundError: No module named 'X'"

**Решение:**
```bash
pip install X
```

Где X - название модуля (например: streamlit, pandas, numpy, plotly)

### ❌ Ошибка: "ImportError: cannot import name 'X'"

**Решение:** 
Это значит, что модуль есть, но функция/класс не найден. 
Обычно это не критично - приложение будет работать, но без этой функции.

### ❌ Ошибка: "NameError: name 'X' is not defined"

**Решение:**
Проверьте, что все импорты в начале файла app.py корректны.

### ❌ Ошибка: "AttributeError: 'NoneType' object has no attribute"

**Решение:**
Это значит, что какой-то модуль не загрузился. 
Проверьте файл config.py и наличие API ключей.

---

## Если ошибки в интерфейсе Streamlit

### Красные сообщения в браузере

1. **"❌ Модуль недоступен"**
   - Это предупреждение, не критично
   - Приложение будет работать, но без этой функции

2. **"❌ API ключ не найден"**
   - Откройте файл `config.py`
   - Добавьте API ключи

3. **"❌ Ошибка загрузки"**
   - Проверьте интернет-соединение
   - Проверьте API ключи

---

## Полная переустановка (если ничего не помогает)

```bash
# 1. Удалите виртуальное окружение
rm -rf venv

# 2. Создайте новое
python3 -m venv venv

# 3. Активируйте
source venv/bin/activate

# 4. Установите все зависимости
pip install -r requirements.txt

# 5. Запустите
streamlit run app.py
```

---

## Проверка после исправления

```bash
python check_app.py
```

Если все ✅ зеленые - приложение готово к запуску!

