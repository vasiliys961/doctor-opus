# Быстрый старт: локальная версия Next.js

## ⚠️ ВАЖНО: Настройка переменных окружения

Для работы локальной версии **обязательно** нужно создать файл `.env.local` с API ключами.

### Шаг 1: Создайте файл `.env.local`

В корне проекта создайте файл `.env.local` со следующим содержимым:

```bash
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-от-openrouter
ASSEMBLYAI_API_KEY=ваш-ключ-от-assemblyai
```

**Где взять ключи:**
- OpenRouter: https://openrouter.ai/keys
- AssemblyAI: https://www.assemblyai.com/app/account

### Шаг 2: Установите зависимости

```bash
npm install
```

### Шаг 3: Запустите локальный сервер

```bash
npm run dev
```

### Шаг 4: Откройте в браузере

```
http://localhost:3000
```

## 🔧 Отладка проблем

### Проблема: "Ошибка запроса" или пустой ответ

**Решение:**
1. Проверьте, что файл `.env.local` существует в корне проекта
2. Убедитесь, что ключи заполнены (не пустые)
3. **Перезапустите сервер** после изменения `.env.local`:
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

### Проблема: "OPENROUTER_API_KEY не настроен"

**Решение:**
- Проверьте, что в `.env.local` есть строка `OPENROUTER_API_KEY=sk-or-v1-...`
- Убедитесь, что нет пробелов вокруг `=`
- Перезапустите сервер

### Проверка переменных окружения

В терминале где запущен `npm run dev` должны быть логи:
```
Calling OpenRouter API: { hasApiKey: true, ... }
```

Если `hasApiKey: false` - переменные окружения не загружены.

## 📝 Отличия от Vercel

- **Локально:** переменные из `.env.local` (файл в проекте, НЕ коммитится в Git)
- **Vercel:** переменные из Dashboard → Settings → Environment Variables

**Важно:** `.env.local` уже добавлен в `.gitignore`, поэтому он не попадёт в Git.

## 🚀 После настройки

1. Откройте http://localhost:3000
2. Попробуйте отправить сообщение в чат
3. Попробуйте загрузить изображение для анализа

Если всё работает - отлично! Если нет - проверьте консоль браузера (F12) и терминал.

