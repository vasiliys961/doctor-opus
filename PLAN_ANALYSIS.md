# Анализ плана: Feedback Loop + Локальное дообучение

## Дата анализа: 2025-01-08

---

## 🔍 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ И НЕСООТВЕТСТВИЯ

### 1. КОНФЛИКТ МЕЖДУ ДВУМЯ ФОРМАМИ ОБРАТНОЙ СВЯЗИ

**Проблема:**
- Существующая форма: `utils/feedback_widget.py` (используется в app.py)
- Новая форма: `feedback/feedback_form.py` (предоставлена в плане)

**Конфликты:**
- **Разные сигнатуры:**
  - Старая: `show_feedback_form(analysis_type: str, analysis_result: str, analysis_id: Optional[str] = None)`
  - Новая: `show_feedback_form(model_output: str, input_text: str)`

- **Разные форматы данных:**
  - Старая сохраняет в БД через `utils.feedback_manager.save_feedback()`
  - Новая сохраняет в JSON файлы в `data/raw_feedback/`

- **Разные поля:**
  - Старая: `analysis_type`, `analysis_id`, чекбокс "неправильный диагноз", комментарий
  - Новая: `correctness` (радио-кнопки: "✅ Полностью верно", "⚠️ Частично верно", "❌ Ошибка"), `specialty`, согласие на использование

**Решение:**
1. **Вариант А (рекомендуется):** Расширить существующую форму, добавив новые поля из новой формы
2. **Вариант Б:** Заменить старую форму новой, но адаптировать сигнатуру под существующие вызовы
3. **Вариант В:** Создать обертку, которая поддерживает оба формата

---

### 2. ОТСУТСТВИЕ ПОЛЯ "СПЕЦИАЛЬНОСТЬ" В ТЕКУЩЕЙ ФОРМЕ

**Проблема:**
- Новая форма требует поле `specialty` для обучения
- Существующая форма его не собирает
- База данных `analysis_feedback` не имеет поля `specialty`

**Решение:**
- Добавить поле `specialty` в таблицу БД (миграция)
- Добавить поле в существующую форму
- Обеспечить обратную совместимость (поле опциональное)

---

### 3. РАЗНЫЕ ФОРМАТЫ СОХРАНЕНИЯ ДАННЫХ

**Текущая система:**
- Данные сохраняются только в SQLite БД (`analysis_feedback`)
- Формат: структурированные поля

**Новая система:**
- Данные сохраняются в JSON файлы (`data/raw_feedback/*.json`)
- Формат: `{"timestamp": "...", "input_case": "...", "model_output": "...", "correctness": "...", "specialty": "..."}`

**Проблема:**
- Два разных формата хранения
- Необходимость синхронизации между БД и файлами

**Решение:**
- Сохранять в оба места одновременно:
  1. В БД (для текущей системы статистики)
  2. В JSON файлы (для подготовки к обучению)
- Или создать единый формат и преобразовывать при необходимости

---

### 4. ОТСУТСТВИЕ ПОЛЯ "СОГЛАСИЕ" В ТЕКУЩЕЙ СИСТЕМЕ

**Проблема:**
- Новая форма требует согласие пользователя (`consent: True/False`)
- Существующая форма его не собирает
- Юридический аспект: использование данных без явного согласия

**Решение:**
- Добавить чекбокс согласия в существующую форму
- Сохранять в БД (новое поле или использовать существующий механизм)
- Обязательное поле перед сохранением

---

### 5. ОТСУТСТВИЕ ПОЛЯ "INPUT_CASE" В ТЕКУЩЕЙ СИСТЕМЕ

**Проблема:**
- Новая форма требует `input_text` (входной кейс пациента)
- Существующая форма его не сохраняет
- Для обучения нужны пары: (входной кейс, правильный ответ)

**Решение:**
- Добавить поле в БД для хранения входного кейса
- Передавать `input_text` в форму (нужно найти источник в app.py)
- Для существующих отзывов входной кейс может отсутствовать

---

### 6. РАЗНЫЕ ТИПЫ ОБРАТНОЙ СВЯЗИ

**Текущая система:**
- `feedback_type`: "incorrect_diagnosis", "needs_improvement", "general_feedback"

**Новая система:**
- `correctness`: "✅ Полностью верно", "⚠️ Частично верно", "❌ Ошибка"

**Проблема:**
- Несоответствие значений
- Маппинг между системами

**Решение:**
- Маппинг:
  - "✅ Полностью верно" → можно не сохранять или сохранять как "correct" (если добавить)
  - "⚠️ Частично верно" → "needs_improvement"
  - "❌ Ошибка" → "incorrect_diagnosis"

---

### 7. ОТСУТСТВИЕ СКРИПТА SETUP.SH

**Проблема:**
- В плане упоминается `setup.sh` для создания папок
- Файла не существует

**Решение:**
- Создать `setup.sh` с командами:
  ```bash
  mkdir -p feedback training data/raw_feedback data/anonymized_cases data/training_data data/models
  ```

---

### 8. НЕСООТВЕТСТВИЕ В REQUIREMENTS.TXT

**Уже установлено:**
- `pandas==2.1.3` ✅ (уже есть)
- `pydantic==2.11.7` ✅ (уже есть)
- `GitPython==3.1.45` ✅ (уже есть)
- `scikit-learn==1.7.1` ✅ (уже есть)

**Нужно добавить:**
- `jsonlines>=4.0` ⏳ (новое)
- `regex>=2024.1.0` ⏳ (новое)
- `torch>=2.0` ⏳ (закомментирован, нужно раскомментировать)
- `transformers>=4.35` ⏳ (закомментирован, нужно раскомментировать)
- `peft>=0.4.0` ⏳ (новое)
- `trl>=0.7.0` ⏳ (новое)
- `accelerate>=0.20.0` ⏳ (новое)
- `datasets>=2.14.0` ⏳ (новое)
- `bitsandbytes>=0.41.0` ⏳ (опционально, для 8-bit)

---

### 9. ОБРЕЗАННЫЙ КОД LORA_FINETUNE.PY

**Проблема:**
- Код `training/lora_finetune.py` обрезан
- Функция `finetune_flash()` не завершена

**Что нужно дописать:**
- Загрузка модели через `AutoModelForCausalLM.from_pretrained()`
- Токенизация данных
- Настройка `TrainingArguments`
- Создание `Trainer`
- Обучение
- Сохранение весов

---

### 10. ОТСУТСТВИЕ .GITIGNORE ДЛЯ RAW_FEEDBACK

**Проблема:**
- В плане указано добавить `data/raw_feedback/` в `.gitignore`
- Текущий `.gitignore` его не содержит

**Решение:**
- Добавить в `.gitignore`:
  ```
  # Feedback system - raw data (contains PII)
  data/raw_feedback/
  data/models/
  *.ckpt
  ```

---

### 11. ИНТЕГРАЦИЯ С СУЩЕСТВУЮЩИМИ РАЗДЕЛАМИ

**Проблема:**
- В `app.py` форма вызывается как: `show_feedback_form(analysis_type="ECG", analysis_result=result, analysis_id="...")`
- Новая форма требует: `show_feedback_form(model_output=result, input_text=user_input)`

**Что нужно:**
- Найти источник `input_text` для каждого раздела:
  - ЭКГ: `image_array` или метаданные?
  - Рентген/МРТ/КТ/УЗИ: `image_array` или метаданные?
  - Лаборатория: входной файл или текст?
  - Генетика: входной файл или текст?
  - Видео: входной файл?

**Решение:**
- Создать функцию-обертку, которая преобразует существующий формат в новый
- Или адаптировать новую форму под существующие параметры

---

### 12. ОТСУТСТВИЕ МОДУЛЯ INFERENCE.PY

**Проблема:**
- В плане указано создать `training/inference.py`
- Код не предоставлен

**Что нужно создать:**
- `load_finetuned_model()` - загрузка модели + LoRA
- `generate_with_finetuned_model()` - генерация ответа
- Интеграция с существующими промптами

---

### 13. ПРОБЛЕМА С МОДЕЛЬЮ GEMMA-2-2B-IT

**Проблема:**
- В плане указано использовать `google/gemma-2-2b-it`
- Это языковая модель, не vision модель
- Для анализа изображений (ЭКГ, рентген, МРТ) нужна vision модель

**Вопрос:**
- Дообучение будет для текстовых ответов (после vision анализа)?
- Или нужно использовать другую модель?
- Или дообучение только для текстовых разделов (лаборатория, генетика)?

**Решение:**
- Уточнить, для каких разделов планируется дообучение
- Если для vision - использовать vision модель (например, `google/gemini-2.5-flash`)
- Если для текстовых ответов - Gemma подходит

---

### 14. ОТСУТСТВИЕ МОДУЛЯ DASHBOARD.PY

**Проблема:**
- В плане указано создать `training/dashboard.py`
- Код не предоставлен (только пример в инструкции)

**Что нужно:**
- Streamlit страница со статистикой
- Визуализация данных
- Интеграция в меню app.py

---

### 15. ПРОБЛЕМА С МАППИНГОМ АНАЛИЗОВ

**Текущая система:**
- `analysis_type`: "ECG", "XRAY", "MRI", "CT", "ULTRASOUND", "DERMATOSCOPY", "LAB", "GENETICS", "VIDEO"

**Новая система (в prepare_training_data.py):**
- Использует `specialty`: "Кардиология", "Онкология", и т.д.

**Проблема:**
- `analysis_type` ≠ `specialty`
- Нужен маппинг между ними

**Решение:**
- Создать маппинг:
  - ECG → Кардиология
  - XRAY/CT → Радиология
  - MRI → Неврология / Радиология
  - DERMATOSCOPY → Дерматология / Онкология
  - И т.д.
- Или использовать `analysis_type` как основу, а `specialty` как дополнительное поле

---

## ✅ ЧТО УЖЕ ЕСТЬ И РАБОТАЕТ

1. ✅ Существующая система обратной связи (`utils/feedback_widget.py`, `utils/feedback_manager.py`)
2. ✅ База данных с таблицей `analysis_feedback`
3. ✅ Интеграция формы во все разделы анализа
4. ✅ Часть зависимостей уже установлена (pandas, pydantic, GitPython, scikit-learn)
5. ✅ Структура проекта готова для расширения

---

## 📋 РЕКОМЕНДАЦИИ ПО ИНТЕГРАЦИИ

### Подход 1: Гибридный (рекомендуется)

**Идея:** Сохранять данные в оба формата, расширить существующую форму

1. Расширить `utils/feedback_widget.py`:
   - Добавить поле "Специальность врача"
   - Добавить радио-кнопки "Оценка корректности"
   - Добавить чекбокс "Согласие на использование"
   - Добавить сохранение входного кейса

2. Расширить `utils/feedback_manager.py`:
   - Добавить функцию `save_feedback_to_file()` для сохранения в JSON
   - Сохранять одновременно в БД и в файлы

3. Адаптировать `feedback/anonymizer.py`:
   - Использовать данные из БД или из JSON файлов

### Подход 2: Параллельный

**Идея:** Новая форма работает параллельно со старой

1. Создать `feedback/feedback_form.py` с новой логикой
2. В `app.py` использовать обе формы (старая для БД, новая для обучения)
3. Периодически синхронизировать данные

### Подход 3: Замена

**Идея:** Заменить старую форму новой, но адаптировать сигнатуру

1. Заменить `utils/feedback_widget.py` на новую логику
2. Адаптировать сигнатуру под существующие вызовы
3. Мигрировать данные из БД в новый формат

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ ДЛЯ УТОЧНЕНИЯ

### Вопросы:

1. **Модель для дообучения:**
   - Какую модель использовать? (Gemma для текста, Gemini для vision?)
   - Для каких разделов планируется дообучение?

2. **Источник input_text:**
   - Откуда брать входной кейс для каждого раздела?
   - Нужно ли сохранять изображения или достаточно метаданных?

3. **Стратегия сохранения:**
   - Сохранять в оба формата (БД + файлы)?
   - Или выбрать один формат и преобразовывать?

4. **Обратная совместимость:**
   - Нужно ли поддерживать старые отзывы?
   - Как обрабатывать отзывы без новых полей (specialty, consent, input_text)?

5. **Интеграция с промптами:**
   - Как интегрировать дообученную модель с существующими промптами?
   - Нужно ли модифицировать `claude_assistant.py`?

---

## 📊 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### Высокий приоритет (критично для работы):
1. ✅ Разрешить конфликт между двумя формами
2. ✅ Добавить недостающие поля в БД
3. ✅ Реализовать сохранение в оба формата (БД + файлы)
4. ✅ Завершить код `lora_finetune.py`

### Средний приоритет (для полной функциональности):
5. ⏳ Создать `inference.py`
6. ⏳ Создать `dashboard.py`
7. ⏳ Обновить `requirements.txt`
8. ⏳ Обновить `.gitignore`

### Низкий приоритет (опционально):
9. ⏳ Создать `setup.sh`
10. ⏳ Оптимизировать процесс анонимизации
11. ⏳ Добавить автоматизацию (скрипты синхронизации)

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПЛАН ДЕЙСТВИЙ

### Этап 1: Подготовка и анализ (30 мин)
- [ ] Определить стратегию интеграции (гибридный/параллельный/замена)
- [ ] Составить список всех мест, где вызывается форма
- [ ] Определить источник `input_text` для каждого раздела

### Этап 2: Расширение существующей системы (2-3 часа)
- [ ] Расширить таблицу БД (добавить поля: specialty, consent, input_case)
- [ ] Расширить `utils/feedback_widget.py` (новые поля)
- [ ] Расширить `utils/feedback_manager.py` (сохранение в файлы)
- [ ] Протестировать сохранение данных

### Этап 3: Создание новых модулей (2-3 часа)
- [ ] Создать структуру папок
- [ ] Скопировать готовые файлы (anonymizer, feedback_form, prepare_training_data)
- [ ] Завершить `lora_finetune.py`
- [ ] Создать `inference.py`
- [ ] Обновить `requirements.txt` и `.gitignore`

### Этап 4: Интеграция (1-2 часа)
- [ ] Интегрировать новую логику сохранения в существующую форму
- [ ] Протестировать полный цикл (сбор → анонимизация → подготовка → обучение)
- [ ] Создать dashboard (опционально)

### Этап 5: Тестирование (1-2 часа)
- [ ] Создать тестовые отзывы
- [ ] Протестировать анонимизацию
- [ ] Протестировать подготовку данных
- [ ] Протестировать дообучение на тестовых данных

---

## 💡 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Не ломать существующую систему:** Форма обратной связи уже работает и используется. Нужно расширить, а не заменить.

2. **Обратная совместимость:** Старые отзывы в БД должны оставаться валидными.

3. **Двойное сохранение:** Рекомендуется сохранять данные и в БД (для текущей системы), и в файлы (для обучения).

4. **Безопасность:** Сырые отзывы с ПИ не должны попадать в git.

5. **Тестирование:** Каждый этап нужно тестировать отдельно перед интеграцией.

---

## ❓ ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

1. Какую модель использовать для дообучения? (Gemma для текста, Gemini для vision, или обе?)

2. Для каких разделов планируется дообучение? (только текстовые или все?)

3. Какой источник `input_text` для каждого раздела? (метаданные, файлы, текст?)

4. Нужно ли сохранять изображения для обучения или достаточно текстовых описаний?

5. Стратегия интеграции: гибридный, параллельный или замена?

6. Нужен ли dashboard сразу или можно отложить?

7. Нужна ли автоматизация (скрипты синхронизации) или достаточно ручного запуска?

---

**План готов к обсуждению. Все найденные проблемы и вопросы задокументированы.**









