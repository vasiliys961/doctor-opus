# Исправления v4.5.1 - Критические баги

**Дата:** 13 февраля 2026  
**Версия:** 4.5.1 (Bugfix Release)

---

## 🐛 Исправленные проблемы

### 1. ❌ "Клинические гипотезы" попадали в скачиваемый DOCX

**Проблема:**
При скачивании протокола в DOCX формате раздел "Клинические гипотезы" (Differential Diagnosis) попадал в файл, хотя должен был быть исключён как внутренняя информация для врачей.

**Причина:**
В массиве `excludeSections` отсутствовали следующие ключевые слова:
- `'клинические гипотезы'`
- `'differential diagnosis'`
- `'дифференциальная диагностика'`

**Файл:** `components/AnalysisResult.tsx` (строки 141-157)

**Исправление:**
```typescript
const excludeSections = [
  'клинический обзор',
  'ведущий синдром',
  'клинический сценарий',
  'оценка рисков',
  'ориентировочная тактика',
  'тактика ведения',
  'противоречия и ограничения',
  'юридический статус',
  'интегрированная сводка',
  'технические параметры',
  'клинические гипотезы',           // ✅ ДОБАВЛЕНО
  'differential diagnosis',          // ✅ ДОБАВЛЕНО
  'дифференциальная диагностика',    // ✅ ДОБАВЛЕНО
  'дифференциальный диагноз',        // ✅ ДОБАВЛЕНО
];
```

**Результат:**  
✅ Теперь в скачиваемом DOCX только: **Протокол описания** и **Заключение**  
✅ Клинические гипотезы остаются в интерфейсе для внутреннего использования

---

### 2. ❌ Пропал расчёт стоимости (единиц) в логах терминала

**Проблема:**
После оптимизации `max_tokens` перестала отображаться информация о стоимости запросов к AI-ассистенту в логах терминала:

```bash
# БЫЛО (до оптимизации):
💰 ОТЧЕТ ОБ ИСПОЛЬЗОВАНИИ
🤖 Модель: claude-opus-4.6
📊 Токены: 3500 (Вход: 1740 / Выход: 1760)
💎 Единицы: 2.5
💵 USD: $0.025

# СТАЛО (после оптимизации):
📊 [ADAPTIVE TOKENS] Режим: chat, Вход: ~1740 токенов
✅ [ADAPTIVE TOKENS] Установлен лимит: 8000 токенов
POST /api/chat 200 in 22499ms
# ❌ НЕТ ИНФОРМАЦИИ О СТОИМОСТИ
```

**Причина:**
В streaming режиме OpenRouter не всегда отправляет `data.usage` в SSE-потоке. Функция `createTransformWithUsage` ожидала usage от API, но не имела fallback-логики для расчёта примерной стоимости.

**Файл:** `lib/openrouter-streaming.ts` (функция `createTransformWithUsage`, строки 25-132)

**Исправление:**

1. **Добавлена переменная для отслеживания:** `usageReceived` и `totalContent`
2. **Сбор контента для fallback-расчёта:** Весь текст ответа собирается в `totalContent`
3. **Fallback-логика:** Если `data.usage` не пришёл, при завершении потока:
   - Оценивается количество токенов по длине ответа (`estimateTokens()`)
   - Рассчитывается стоимость
   - Выводится отчёт с пометкой `[STREAMING FALLBACK]`

```typescript
// ДО исправления:
function createTransformWithUsage(stream, model, initialUsage) {
  // ... код ...
  if (done) break; // ❌ Сразу выход, нет fallback
}

// ПОСЛЕ исправления:
function createTransformWithUsage(stream, model, initialUsage) {
  let usageReceived = false;
  let totalContent = ''; // Собираем контент
  
  // ... в цикле ...
  if (data.choices?.[0]?.delta?.content) {
    totalContent += data.choices[0].delta.content; // ✅ Собираем
  }
  if (data.usage) {
    usageReceived = true; // ✅ Пометка
  }
  
  if (done) {
    // ✅ Fallback если usage не пришёл
    if (!usageReceived && totalContent) {
      const approxOutputTokens = estimateTokens(totalContent);
      // Расчёт и логирование стоимости
      console.log(`✅ [STREAMING FALLBACK] Анализ завершен (${model})`);
      console.log(`   📊 ИТОГО (примерно): ${formatCostLog(...)}`);
    }
    break;
  }
}
```

**Результат:**  
✅ Теперь стоимость **ВСЕГДА** отображается в логах  
✅ Если usage пришёл от API → точные данные  
✅ Если usage не пришёл → примерный расчёт с пометкой "FALLBACK"

---

## 📊 Примеры логов после исправления

### Если usage пришёл от API (точно):
```bash
📊 [ADAPTIVE TOKENS] Режим: chat, Вход: ~1740 токенов
✅ [ADAPTIVE TOKENS] Установлен лимит: 8000 токенов
✅ [STREAMING] Анализ завершен успешно (anthropic/claude-opus-4.6)
   📊 ИТОГО:
╔══════════════════════════════════════════════════════════════╗
║                 💰 ОТЧЕТ ОБ ИСПОЛЬЗОВАНИИ                     ║
╠══════════════════════════════════════════════════════════════╣
║ 🤖 Модель:   claude-opus-4.6                              ║
║ 📊 Токены:   3,500                                        ║
║    (Вход: 1,740 / Выход: 1,760)                          ║
╠══════════════════════════════════════════════════════════════╣
║ 💎 Единицы:  2.50                                         ║
║ 💵 USD:      $0.0250                                      ║
╚══════════════════════════════════════════════════════════════╝
POST /api/chat 200 in 22499ms
```

### Если usage не пришёл (fallback):
```bash
📊 [ADAPTIVE TOKENS] Режим: chat, Вход: ~1740 токенов
✅ [ADAPTIVE TOKENS] Установлен лимит: 8000 токенов
✅ [STREAMING FALLBACK] Анализ завершен (anthropic/claude-opus-4.6)
   📊 ИТОГО (примерно):
╔══════════════════════════════════════════════════════════════╗
║                 💰 ОТЧЕТ ОБ ИСПОЛЬЗОВАНИИ                     ║
╠══════════════════════════════════════════════════════════════╣
║ 🤖 Модель:   claude-opus-4.6                              ║
║ 📊 Токены:   ~3,450                                       ║
║    (Вход: 1,740 / Выход: ~1,710)                         ║
╠══════════════════════════════════════════════════════════════╣
║ 💎 Единицы:  ~2.48                                        ║
║ 💵 USD:      ~$0.0248                                     ║
╚══════════════════════════════════════════════════════════════╝
POST /api/chat 200 in 22499ms
```

---

## 🧪 Тестирование

### Тест 1: Проверка excludeSections
1. Отправить изображение на анализ
2. Дождаться протокола с "Клиническими гипотезами"
3. Нажать "📄 Скачать .docx"
4. ✅ **Ожидание:** В DOCX НЕТ раздела "Клинические гипотезы"

### Тест 2: Проверка логирования стоимости
1. Открыть AI-ассистента (чат)
2. Отправить любое сообщение
3. Открыть терминал (`/terminals/1.txt`)
4. ✅ **Ожидание:** Виден отчёт "💰 ОТЧЕТ ОБ ИСПОЛЬЗОВАНИИ"

---

## ✅ Результаты

### TypeScript компиляция:
```bash
npx tsc --noEmit
# Результат: 11 ошибок (все pre-existing, не связаны с исправлениями)
```

### Изменённые файлы:
1. `components/AnalysisResult.tsx` - добавлено 4 строки в excludeSections
2. `lib/openrouter-streaming.ts` - добавлен fallback для расчёта стоимости (~50 строк)

### Побочные эффекты:
✅ Нет - все изменения обратно совместимы

---

## 📝 Changelog

### v4.5.1 (13.02.2026) - Bugfix Release
- 🐛 Исправлено: "Клинические гипотезы" теперь корректно исключаются из DOCX
- 🐛 Исправлено: Восстановлено логирование стоимости в терминале (с fallback)
- ✅ TypeScript: без новых ошибок компиляции
- ✅ Обратная совместимость: полная

---

## 👥 Отзывы

> ✅ **Профессор:** "Отлично!"  
> (После отправки документа - гипотезы корректно исключены)

---

**Doctor Opus AI Team**  
v4.5.1 (Bugfix Release) - 13 февраля 2026
